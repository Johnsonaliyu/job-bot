[phases.setup]
nixPkgs = [
  "nodejs_20",
  "chromium",
  "nss",
  "atk",
  "cups",
  "gtk3",
  "libdrm",
  "mesa",
  "libxkbcommon",
  "glib"
]

[phases.install]
cmds = ["npm install --omit=dev"]

[start]
cmd = "node index.js"
